x + y
add <- function(x, y=1){
x + y
}
add(x=4)
add(x=4, y=10)
add(4)
add(4, 20)
add(c(1, 2, 5, 10) y=10)
add(c(1, 2, 5, 10), y=10)
add(c(1,2,5,10), 10, 20)
source("~/.active-rstudio-document", echo=TRUE)
# Test on a simple example
rescale(1:10)
rescale <- function(x) {
rng <- range(x)
(x- rng[1] / rng[2] - rng[1])
rescale(1:10)
rescale <- function(x) {
rng <- range(x)
(x- rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <- range(x)
(x- rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <- range(x)
(x- rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <- range(x)
(x- rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <- range(x)
(x- rng[1] / rng[2] - rng[1])
}
rng <-range(x)
rescale <-function(x) {
rng <-range(x)
(x- rng[1] / rng[2] - rng[1])
}
rescale <-function(x) {
rng <-range(x)
(x- rng[1] / rng[2] - rng[1])
}
rescale <- function(x){
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x){
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x){
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x){
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x){
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rng <-range(x)
rescale <- function(x) {
rescale <- function(x) {
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
rescale <- function(x) {
rng <-range(x)
(x - rng[1] / rng[2] - rng[1])
}
# Test on a simple example
rescale(1:10)
rescale(1:10)
rescale( c(1,4, 10, 20))
source("rescale02.R")
source("rescale02.R")
rescale <- function(x)
(x - rng[1] / (rng[2] - rng[1]))
rescale(1:10)
rng <-range(x)
rescale <-function(x) {
rng <-range(x)
(x - rng[1] / (rng[2] - rng[1]))
}
rescale(1:10)
rescale( c(1, NA, 4, 10, 20))
rescale( c(1, NA, 4, 10, 20), na.rm = TURE)
rescale( c(1, NA, 4, 10, 20), na.rm = TRUE)
rescale01 <- function(x) {
rng <- range(x, na.rm=TRUE)
(x - rng[1]) / (rng[2] - rng[1])
}
rescale01( c(1, NA, 4, 10, 20))
rescale01( c(1, NA, 4, 10, "word'))
rescale01( c(1, NA, 4, 10, "word'))
rescale02 <- function(x, na.rm=TRUE) {
rng <- range(x, na.rm=na.rm)
(x - rng[1]) / (rng[2] - rng[1])
}
source("rescale02.R")
source("rescale02")
source("C:/Users/RALPH/Desktop/Winter 2022/bimm143/week06/functionr/rescale02.R", echo=TRUE)
rescale02 <- function(x, na.rm=TRUE) {
rng <- range(x, na.rm=na.rm)
(x - rng[1]) / (rng[2] - rng[1])
}
source("C:/Users/RALPH/Desktop/Winter 2022/bimm143/week06/functionr/rescale02.R", echo=TRUE)
source(rescale02.R)
source("rescale02.R")
rescale02( 1:10 )
rescale( 1:10, plot = TRUE)
